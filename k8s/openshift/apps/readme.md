# openshift apps

this folder contains the openshift apps manifests and templates. The manifests are used to deploy the apps in openshift cluster. The templates are used to create the apps in openshift cluster.

